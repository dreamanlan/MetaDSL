[[13]]
int
main ()
{
    //fdafdsa

    /*fdafa
    fdafads
    fdasfa
    */

   //-----12345-----

  struct index_st
  {
    unsigned char type;
    unsigned char num;
    unsigned int len;
  };
#if 1
  typedef struct index_st index_t;
  int x,y;
  x = offsetof(struct index_st, len);
  y = offsetof(index_t, num)

  ;
#define ABC abc\
fdef

#define DEF abc(abc)

#define XXX 1 > 2 ? 1 : 0

#endif  
  return 0;
};

#define xxx 1

abc(()abcd);